底层微服务，服务的提供方

1.本实例中模拟了如下几种案例

1.GET方法，输入简单参数，返回简单参数，这种可以不用测试了

2.GET方法，输入简单参数，返回复杂对象，测试通过，可以被上游服务调用

3.GET方法，输入复杂参数，返回复杂对象，浏览器不好模拟，上游调用不成功，传入参数变成了新对象，属性都是null,返回值按正常逻辑返回，也是新对象
    说明这种方法不好

4.POST方法，输入简单参数，返回简单参数，无测试

5.POST方法，输入复杂参数，返回复杂对象，测试通过，上游可以调用

6.关于MAP，LIST，SET参数做为输入，需要用POST方法，上游才可以调用，虽然浏览器可以模拟，但是需要表达的模式才行

综上所述，如果参数是复杂对象，LIST，MAP，SET建议使用POST方法，尤其要给其他服务调用的时候，一定要用POST方法

另外两个模拟

7.抛出异常模拟

8.请求超时模拟